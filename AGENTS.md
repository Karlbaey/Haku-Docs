使用 PowerShell 7.0 指令。**在删除任何文件前必须征询我的意见。**

这是一个 Hugo 驱动的文档站，主推简洁，易读，轻量。

严禁使用 `color-mix()` 函数，应该在 `assets/css/global.css` 中定义好 CSS 变量后直接取用。

**语言**: 默认中文（代码/命令/日志保持原语言）
**环境**: Windows + UTF-8 编码 + PowerShell 7